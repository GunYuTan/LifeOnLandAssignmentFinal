<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <ImageView
        android:id="@+id/smallNotificationIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_notification" />

    <TextView
        android:id="@+id/smallNotificationTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/smallNotificationIcon"
        android:layout_marginStart="8dp"
        android:text="Small Notification Title"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/smallNotificationContent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/smallNotificationTitle"
        android:layout_toEndOf="@id/smallNotificationIcon"
        android:layout_marginStart="8dp"
        android:text="Content here..." />

</RelativeLayout>